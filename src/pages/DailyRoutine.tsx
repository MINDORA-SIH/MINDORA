import { DndContext, KeyboardSensor, PointerSensor, TouchSensor, closestCenter, useSensor, useSensors } from "@dnd-kit/core";
import { SortableContext, arrayMove, sortableKeyboardCoordinates, useSortable, verticalListSortingStrategy } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";
import { Check, ChevronLeft, ChevronDown, ChevronUp, GripVertical, Lightbulb, ListChecks, RotateCcw, Trophy } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router";
import { fallbackRoutine, saveRoutineSession } from "@/data/routineRepository";
import { DEFAULT_PATIENT, categoryLabel, shuffleSteps, stepsForDifficulty, type Routine, type RoutineGameSession, type RoutineStep } from "@/data/routineTypes";
import { useRoutines } from "@/hooks/useRoutines";

function GameStep({ step, index, count, showCue, onMove }: { step: RoutineStep; index: number; count: number; showCue: boolean; onMove: (direction: -1 | 1) => void }) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: step.id });
  return <li ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }} className={`flex items-center gap-3 rounded-2xl border-2 p-3 sm:p-4 ${isDragging ? "border-[#0F6E56] bg-[#E1F5EE] shadow-lg dark:bg-[#1E3B33]" : "border-slate-200 bg-white dark:border-slate-700"}`}>
    <button type="button" {...attributes} {...listeners} className="tap-target shrink-0 rounded-xl text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-700" aria-label={`Drag ${step.title}`}><GripVertical /></button><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-[#E1F5EE] text-[17px] font-extrabold text-[#0F6E56] dark:bg-[#1E3B33] dark:text-[#8FE3C8]">{index + 1}</span><div className="min-w-0 flex-1"><p className="text-[19px] font-extrabold" style={{ color: "var(--foreground)" }}>{showCue ? `${step.icon ?? "✅"} ` : ""}{step.title}</p>{showCue && step.scheduledTime ? <p className="text-[14px] font-bold" style={{ color: "var(--muted)" }}>{step.scheduledTime}</p> : null}</div><div className="flex shrink-0 gap-1"><button type="button" onClick={() => onMove(-1)} disabled={index === 0} aria-label={`Move ${step.title} up`} className="tap-target rounded-xl border border-slate-200 disabled:opacity-35 dark:border-slate-700"><ChevronUp size={19} /></button><button type="button" onClick={() => onMove(1)} disabled={index === count - 1} aria-label={`Move ${step.title} down`} className="tap-target rounded-xl border border-slate-200 disabled:opacity-35 dark:border-slate-700"><ChevronDown size={19} /></button></div></li>;
}

function RoutineGame({ routine, onBack }: { routine: Routine; onBack: () => void }) {
  const [steps, setSteps] = useState<RoutineStep[]>(() => shuffleSteps(stepsForDifficulty(routine))); const [startedAt] = useState(() => Date.now()); const [hints, setHints] = useState(0); const [retries, setRetries] = useState(0); const [errors, setErrors] = useState(0); const [result, setResult] = useState<{ accuracy: number; complete: boolean } | null>(null); const [saved, setSaved] = useState(false);
  const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }), useSensor(TouchSensor, { activationConstraint: { delay: 180, tolerance: 6 } }), useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }));
  const target = useMemo(() => stepsForDifficulty(routine), [routine]); const showCue = routine.difficulty <= 2;
  const reorder = (next: RoutineStep[]) => setSteps(next);
  const check = () => { const correct = steps.reduce((count, step, index) => count + (step.id === target[index]?.id ? 1 : 0), 0); const accuracy = Math.round(correct / Math.max(target.length, 1) * 100); const complete = correct === target.length; setResult({ accuracy, complete }); if (!complete) { setErrors((value) => value + (target.length - correct)); setRetries((value) => value + 1); } };
  useEffect(() => { if (!result?.complete || saved) return; const session: RoutineGameSession = { id: `routine-session-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, patientId: DEFAULT_PATIENT.id, routineId: routine.id, startedAt: new Date(startedAt).toISOString(), completedAt: new Date().toISOString(), difficulty: routine.difficulty, score: result.accuracy, accuracy: result.accuracy, errors, hintsUsed: hints, retries, completionTimeMs: Date.now() - startedAt }; setSaved(true); void saveRoutineSession(session); }, [result, saved, routine, startedAt, errors, hints, retries]);
  const reset = () => { setSteps(shuffleSteps(target)); setResult(null); setHints(0); setRetries(0); setErrors(0); };
  return <div className="space-y-5"><button type="button" onClick={onBack} className="gap-2 text-[18px] font-bold" style={{ color: "var(--muted-strong)" }}><ChevronLeft size={18} /> Choose another routine</button><header className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl border-2 border-[#B6E3C8] bg-[#EDFBF3] text-2xl dark:border-[#27543E] dark:bg-[#12352A]">{routine.category === "morning" ? "🌅" : "🗓️"}</span><div><h1 className="text-2xl font-extrabold">{routine.name}</h1><p className="text-[16px] font-semibold" style={{ color: "var(--muted-strong)" }}>Put the activities in the order you usually do them.</p></div></div><span className="rounded-full border-2 border-[#B6E3C8] bg-[#EDFBF3] px-4 py-1.5 text-[15px] font-extrabold text-[#186B47] dark:border-[#27543E] dark:bg-[#12352A] dark:text-[#8FE3B4]">Level {routine.difficulty} · {target.length} steps</span></header><section className="rounded-3xl border-2 border-slate-200 p-4 shadow-sm sm:p-6 dark:border-slate-700" style={{ backgroundColor: "var(--card-bg)" }}><p className="mb-4 text-[16px] font-semibold" style={{ color: "var(--muted-strong)" }}>Drag an activity, use your keyboard, or use the arrow buttons. Then choose Check my order.</p><DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={({ active, over }) => { if (over && active.id !== over.id) { const from = steps.findIndex((step) => step.id === active.id); const to = steps.findIndex((step) => step.id === over.id); reorder(arrayMove(steps, from, to)); } }}><SortableContext items={steps.map((step) => step.id)} strategy={verticalListSortingStrategy}><ul className="space-y-2">{steps.map((step, index) => <GameStep key={step.id} step={step} index={index} count={steps.length} showCue={showCue} onMove={(direction) => reorder(arrayMove(steps, index, index + direction))} />)}</ul></SortableContext></DndContext><div className="mt-5 flex flex-wrap gap-3"><button type="button" onClick={() => setHints((value) => value + 1)} className="tap-target gap-2 rounded-2xl border-2 border-[#F0D79E] bg-[#FFF7E6] px-5 text-[16px] font-extrabold text-[#8A5B0B] dark:border-[#5E4718] dark:bg-[#3A2C10] dark:text-[#F5CE83]"><Lightbulb size={19} /> Show a hint</button><button type="button" onClick={check} className="tap-target gap-2 rounded-2xl bg-[#0F6E56] px-6 text-[16px] font-extrabold text-white shadow-md"><Check size={19} /> Check my order</button></div>{hints > 0 ? <div className="mt-3 rounded-2xl border border-[#F0D79E] bg-[#FFF7E6] p-3 text-[15px] font-bold text-[#8A5B0B] dark:border-[#5E4718] dark:bg-[#3A2C10] dark:text-[#F5CE83]">Hint: {target.map((step) => step.hint || step.scheduledTime || step.title).join(" → ")}</div> : null}{result ? <div role="status" className={`mt-4 rounded-2xl border-2 p-4 ${result.complete ? "border-[#B6E3C8] bg-[#EDFBF3] dark:border-[#27543E] dark:bg-[#12352A]" : "border-[#F0D79E] bg-[#FFF7E6] dark:border-[#5E4718] dark:bg-[#3A2C10]"}`}><div className="flex items-center gap-3">{result.complete ? <Trophy className="h-8 w-8 text-[#186B47] dark:text-[#8FE3B4]" /> : <RotateCcw className="h-8 w-8 text-[#8A5B0B] dark:text-[#F5CE83]" />}<div><p className="text-[19px] font-extrabold">{result.complete ? "Wonderful work!" : "Almost there — try again."}</p><p className="text-[16px] font-semibold">You placed {result.accuracy}% of the activities in the right spot.</p></div></div>{result.complete ? <button type="button" onClick={reset} className="tap-target mt-3 gap-2 rounded-xl border-2 border-[#B6E3C8] px-4 text-[15px] font-extrabold text-[#186B47] dark:border-[#27543E] dark:text-[#8FE3B4]"><RotateCcw size={17} /> Play again</button> : null}</div> : null}</section></div>;
}

export function DailyRoutine() {
  const navigate = useNavigate(); const { routines, isLoading } = useRoutines(DEFAULT_PATIENT.id, false); const activeRoutines = routines.length ? routines : [fallbackRoutine()]; const [selectedId, setSelectedId] = useState<string | null>(null); const selected = activeRoutines.find((routine) => routine.id === selectedId);
  if (selected) return <RoutineGame routine={selected} onBack={() => setSelectedId(null)} />;
  return <div className="space-y-5"><button type="button" onClick={() => navigate("/")} className="gap-2 text-[18px] font-bold" style={{ color: "var(--muted-strong)" }}><ChevronLeft size={18} /> Back to Games</button><header className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl border-2 border-[#B6E3C8] bg-[#EDFBF3] text-2xl dark:border-[#27543E] dark:bg-[#12352A]">📋</span><div><h1 className="text-2xl font-extrabold">Daily Routine</h1><p className="text-[17px] font-semibold" style={{ color: "var(--muted-strong)" }}>Choose a routine to practise putting activities in order.</p></div></header>{isLoading ? <p className="text-[17px] font-semibold" style={{ color: "var(--muted)" }}>Loading your routines…</p> : <section aria-label="Choose a routine" className="grid grid-cols-1 gap-4 sm:grid-cols-2">{activeRoutines.map((routine) => { const count = stepsForDifficulty(routine).length; return <article key={routine.id} className="rounded-3xl border-2 border-[#B6E3C8] bg-[#EDFBF3] p-5 shadow-sm dark:border-[#27543E] dark:bg-[#12352A]"><div className="flex items-start gap-3"><span className="text-3xl">{routine.category === "morning" ? "🌅" : routine.category === "evening" || routine.category === "bedtime" ? "🌙" : "📋"}</span><div><h2 className="text-[20px] font-extrabold text-[#0F6E56] dark:text-[#8FE3C8]">{routine.name}</h2><p className="text-[16px] font-bold text-[#186B47] dark:text-[#8FE3B4]">{count} activities · {categoryLabel(routine.category)}</p></div></div>{routine.description ? <p className="mt-3 text-[15px] font-semibold" style={{ color: "var(--muted-strong)" }}>{routine.description}</p> : null}<button type="button" onClick={() => setSelectedId(routine.id)} className="tap-target mt-5 w-full gap-2 rounded-2xl bg-[#0F6E56] px-5 text-[17px] font-extrabold text-white"><ListChecks size={20} /> Start</button></article>; })}</section>}</div>;
}
export default DailyRoutine;
